# Agent Bootstrap

Before working in this repository, read
`internal/agent-docs/.ht-AGENTS.md` completely and treat it as the repository's
agent instructions. Resolve project paths from the repository root.

Detailed project context and content-writing guidance are kept beside that
file in protected `.ht-*.md` files under `internal/agent-docs/`.
